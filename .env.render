# Render Environment Variables Template
# Copy this to your Render service environment variables

# Database configuration - This will be overridden by Render if you use their PostgreSQL service
DATABASE_URL=postgresql://streamflix_user:HUdl2cuifBo3lW3YHRzMx9TgYL6gl0Es@dpg-d3di80mr433s73eb0ok0-a.oregon-postgres.render.com/streamflix

# JWT Secret for authentication (generate a secure random string)
JWT_SECRET=your_secure_jwt_secret_here

# TMDB API Key (required for movie data)
TMDB_API_KEY=your_tmdb_api_key

# PayPal Configuration
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret
PAYPAL_MODE=sandbox

# Email Configuration
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password

# Client URL (for redirects) - Will be overridden by Render
CLIENT_URL=https://your-app.onrender.com

# Base URL (for webhooks) - Will be overridden by Render
BASE_URL=https://your-app.onrender.com

# Port configuration - Will be overridden by Render
PORT=10000

# Node environment
NODE_ENV=production